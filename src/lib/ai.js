const generateFlashcard = async (question) => {
    console.log('question from ai.js', question);

    

    return {answer: 'this is a dummy answer from ai.js'};
}

module.exports = generateFlashcard